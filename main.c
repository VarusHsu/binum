#include <stdio.h>
#include <stdlib.h>

typedef long long int number;

void print_usage_then_exit(char*);
void panic_then_exit(char*);

int main(int argc, char** argv) {
    print_usage_then_exit(argv[0]);
    return 0;
}

void print_usage_then_exit(char* argv1) {
    printf("usage: %s num\n", argv1);
    exit(1);
}

int positive(number num, int base, char* buffer) {

    for(int i =0; i< )
    return 0;
}

char* calculate_original_code(number num) {

}